import SwiftUI

extension Color {
    // The signature "Saldo Yellow" - a high contrast neon/acid yellow
    static let saldoYellow = Color(red: 0.85, green: 1.0, blue: 0.2)
    
    // A deep, rich background color (very dark blue/black) to make the glass pop
    static let saldoBackground = Color(red: 0.05, green: 0.07, blue: 0.1)
    
    // Secondary accent for gradients
    static let saldoCyan = Color(red: 0.2, green: 0.9, blue: 1.0)
    
    // Tertiary accent for subtle warmth
    static let saldoPurple = Color(red: 0.7, green: 0.3, blue: 0.9)
}
